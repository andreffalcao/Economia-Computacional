# Estrutura de Controle Sequência - exercício 08
# Entrada de Dados
a = float(input("Digite o valor de 'a': "))
b = float(input("Digite o valor de 'b': "))

# Saída de Dados
if a != 0:
    x = b / a
    print(f"Resposta 8: {x}")
else:
    print("Resposta 8: Equação inválida, dado que a = 0.")
